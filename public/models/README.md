Drop your converted model here as `model_1.glb`.

The app fetches it from `/models/model_1.glb` — this folder (`public/`) is
served as-is by Vite at the site root, so a file at
`public/models/model_1.glb` becomes reachable at `/models/model_1.glb`
with no code changes needed once it's here.
